module.exports = {
  content: ['src/**/*.tsx'],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        white: '#ffffff',
      },
    },
  },
  plugins: [
    // require('postcss-import'),
  ],
};
